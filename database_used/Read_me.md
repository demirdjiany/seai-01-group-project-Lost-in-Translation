this is the database that was used for the project, exported from phpmyadmin as a .sql

You will first need to create a new table called lost_in_translation
Then choose Import and import this .sql

this will also contain the testcases used

you'll need to have xampp installed, run apache and mysql, and open http://localhost/phpmyadmin